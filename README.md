DeepLinking - Clicking a link opens application if its installed on the device , if not redirect the user to playstore/appstore
Redirect to a specific scene in a game by giving the necessary parameters, also by adding a custom manifest file from within unity.
